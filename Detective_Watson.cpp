#include<bits/stdc++.h>
#include<algorithm>
using namespace std;
int main()
{
    while (1)
    {
    int n;
    cin>>n;
    int arr[n];
    for (int i = 0; i < n; i++)
    {
        cin>>arr[i];
    }
    sort(arr,arr+n);
    int k=arr[n-1]
    
    
}